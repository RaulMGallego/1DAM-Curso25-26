#!/bin/bash

echo "🔧 Instalando Jupyter y Java kernel (IJava)..."

# Instala Jupyter y Java kernel
pip install jupyterlab
curl -L https://github.com/SpencerPark/IJava/releases/download/v1.3.0/ijava-1.3.0.zip -o ijava.zip
unzip ijava.zip -d ijava-installer
cd ijava-installer
python install.py --sys-prefix

echo "✅ Kernel de Java instalado correctamente."
