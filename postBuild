#!/bin/bash

echo "🔧 Instalando Maven si no está presente..."
sudo apt-get update
sudo apt-get install -y maven

echo "📦 Compilando proyecto Java con Maven..."
mvn clean install

echo "✅ Compilación completada."
